/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.postimg.cc','cdn.pixabay.com','res.cloudinary.com' ,'bannernn.my.canva.site'],
      },
};

export default nextConfig;
